# CI workflow disabled
# This file is preserved for future use but currently disabled.
# To re-enable, replace the 'on:' block below with the real triggers.

on:
  push:
    branches: [ never-trigger ]  # Dummy branch to prevent triggers
  pull_request:
    branches: [ never-trigger ]  # Dummy branch to prevent triggers

jobs:
  # No-op job to keep workflow syntax valid but do nothing
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "CI/CD is currently disabled."
